.container.text-center
	div.user-info(ng-show='user')
		img(ng-src='{{user.profile_picture}}')
		h5 {{user.username}}

	button.btn.btn-primary.btn-lg(type='button' ng-click='login()')
		span.glyphicon(aria-hidden='true')
		| Login with Instagram
	
